spring:
  application:
    name: API-GATEWAY

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 9000

spring:
  cloud:
    gateway:
      routes:

        - id: authService
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/auth/**

        - id: productService
          uri: lb://PRODUCT-SERVICE
          predicates:
            - Path=/product/**
          filters:
            - StripPrefix=1
